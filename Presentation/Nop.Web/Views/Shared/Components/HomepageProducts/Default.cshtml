@model IList<ProductOverviewModel>
@if (Model.Count > 0)
{

    @*
              <div class="item-grid">
            @foreach (var item in Model)
            {
                <div class="item-box">
                    @await Html.PartialAsync("_ProductBox", item)
                </div>
            }
        </div>
    *@

    @foreach (var item in Model)
    {
        <div class="slide">
            @await Html.PartialAsync("_ProductBox", item)
        </div>
    }
}